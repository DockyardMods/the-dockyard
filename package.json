{
  "name": "the-dockyard",
  "private": true,
  "type": "module",
  "scripts": {
    "format:registry": "bun run scripts/sort-registry.ts && bunx prettier --write registry.json",
    "validate:schema": "bun run scripts/validate-registry-schema.ts",
    "validate": "bun validate:schema"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "prettier": "^3.8.2",
    "temporal-polyfill": "^0.3.2",
    "try": "^1.0.3"
  }
}
