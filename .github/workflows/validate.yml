name: Validate Registry

on:
  pull_request:
    paths:
      - registry.json
  push:
    paths:
      - registry.json

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
